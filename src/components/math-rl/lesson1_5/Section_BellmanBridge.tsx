/**
 * Section_BellmanBridge — мост к Bellman Optimality и Q-Learning.
 * TODO (промпт 10): V*, Q*, π* = argmax_a Q*(s,a), переход к уроку 1.6.
 */
const Section_BellmanBridge = () => {
  return (
    <section
      id="l15-bellman-bridge"
      aria-label="Lesson 1.5 — Bellman Bridge (плейсхолдер)"
      className="py-2"
    >
      <h2
        className="text-2xl font-bold tracking-wide"
        style={{
          fontFamily: "'Orbitron', 'Inter', ui-sans-serif, system-ui, sans-serif",
          color: "#D946EF",
        }}
      >
        Мост к Q-Learning
      </h2>
      <p className="text-sm italic text-muted-foreground mt-1">
        Placeholder — будет заполнено в промпте 10 (V*, Q*, π* и переход к 1.6).
      </p>
    </section>
  );
};

export default Section_BellmanBridge;
