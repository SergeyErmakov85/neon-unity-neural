/**
 * Section_PolicyAndTrace — политика π, траектория τ и возврат G_t.
 * TODO (промпт 6): det/stoch политика, цепочка (s, a, r), discounted return.
 */
const Section_PolicyAndTrace = () => {
  return (
    <section
      id="l15-policy-and-trace"
      aria-label="Lesson 1.5 — Policy & Trace (плейсхолдер)"
      className="py-2"
    >
      <h2
        className="text-2xl font-bold tracking-wide"
        style={{
          fontFamily: "'Orbitron', 'Inter', ui-sans-serif, system-ui, sans-serif",
          color: "#00FFD6",
        }}
      >
        Политика и траектория
      </h2>
      <p className="text-sm italic text-muted-foreground mt-1">
        Placeholder — будет заполнено в промпте 6 (π, τ, G_t, роль γ).
      </p>
    </section>
  );
};

export default Section_PolicyAndTrace;
