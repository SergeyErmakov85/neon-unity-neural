/**
 * Section_ValueQ — функция ценности действия Q^π(s, a).
 * TODO (промпт 8): определение, связь V↔Q, почему model-free учит Q.
 */
const Section_ValueQ = () => {
  return (
    <section
      id="l15-value-q"
      aria-label="Lesson 1.5 — Q-функция (плейсхолдер)"
      className="py-2"
    >
      <h2
        className="text-2xl font-bold tracking-wide"
        style={{
          fontFamily: "'Orbitron', 'Inter', ui-sans-serif, system-ui, sans-serif",
          color: "#D946EF",
        }}
      >
        Q-функция ценности действия
      </h2>
      <p className="text-sm italic text-muted-foreground mt-1">
        Placeholder — будет заполнено в промпте 8 (Q^π(s,a), argmax, связь с DQN).
      </p>
    </section>
  );
};

export default Section_ValueQ;
