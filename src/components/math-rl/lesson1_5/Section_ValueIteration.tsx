/**
 * Section_ValueIteration — алгоритм Value Iteration на gridworld.
 * TODO (промпт 9): Python-код 4×4, визуализация V и π*, сходимость.
 */
const Section_ValueIteration = () => {
  return (
    <section
      id="l15-value-iteration"
      aria-label="Lesson 1.5 — Value Iteration (плейсхолдер)"
      className="py-2"
    >
      <h2
        className="text-2xl font-bold tracking-wide"
        style={{
          fontFamily: "'Orbitron', 'Inter', ui-sans-serif, system-ui, sans-serif",
          color: "#00FFD6",
        }}
      >
        Value Iteration на gridworld
      </h2>
      <p className="text-sm italic text-muted-foreground mt-1">
        Placeholder — будет заполнено в промпте 9 (Python-код + визуализация V* и π*).
      </p>
    </section>
  );
};

export default Section_ValueIteration;
