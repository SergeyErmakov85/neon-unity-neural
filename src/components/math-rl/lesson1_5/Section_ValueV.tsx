/**
 * Section_ValueV — функция ценности состояния V^π(s).
 * TODO (промпт 7): определение через E_π[G_t], уравнение Беллмана (expectation), визуализация.
 */
const Section_ValueV = () => {
  return (
    <section
      id="l15-value-v"
      aria-label="Lesson 1.5 — V-функция (плейсхолдер)"
      className="py-2"
    >
      <h2
        className="text-2xl font-bold tracking-wide"
        style={{
          fontFamily: "'Orbitron', 'Inter', ui-sans-serif, system-ui, sans-serif",
          color: "#00FFD6",
        }}
      >
        V-функция ценности состояния
      </h2>
      <p className="text-sm italic text-muted-foreground mt-1">
        Placeholder — будет заполнено в промпте 7 (V^π(s), Bellman expectation, viz).
      </p>
    </section>
  );
};

export default Section_ValueV;
