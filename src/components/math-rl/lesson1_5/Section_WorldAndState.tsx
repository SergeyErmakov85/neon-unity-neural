/**
 * Section_WorldAndState — мир MDP и его состояние.
 * TODO (промпт 4): определения S, A, T, R, γ; агент ↔ среда; пример state vector.
 */
const Section_WorldAndState = () => {
  return (
    <section
      id="l15-world-and-state"
      aria-label="Lesson 1.5 — World & State (плейсхолдер)"
      className="py-2"
    >
      <h2
        className="text-2xl font-bold tracking-wide"
        style={{
          fontFamily: "'Orbitron', 'Inter', ui-sans-serif, system-ui, sans-serif",
          color: "#00FFD6",
        }}
      >
        Мир и состояние
      </h2>
      <p className="text-sm italic text-muted-foreground mt-1">
        Placeholder — будет заполнено в промпте 4 (S, A, T, R, γ и петля agent ↔ env).
      </p>
    </section>
  );
};

export default Section_WorldAndState;
